[run]
agent = "/bin/sh"
agent_args = ["-c", "printf '{\"event\":\"demo\"}\n'"]
goal = "demo success"
cwd = ".."
timeout_ms = 1000

[profiles.opencode-acp]
agent = "opencode"
agent_args = ["acp"]
goal = "serve acp"
cwd = ".."
timeout_ms = 3000

[profiles.echo-demo]
agent = "/bin/sh"
agent_args = ["-c", "printf '{\"event\":\"demo-profile\"}\n'"]
goal = "demo success"
cwd = ".."
timeout_ms = 1000
